# Standard peer-dependency resolution. This overrides any user-level
# legacy-peer-deps=true so locally generated lockfiles match what a
# stock npm (CI, other machines) expects from `npm ci`.
legacy-peer-deps=false
